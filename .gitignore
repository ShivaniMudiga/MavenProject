target/
.idea/
*.class
*.log
*.iml
.DS_Store


open eclipse 
click on file > import > project from git >clone uri >give uri > choose any directory >
import existing Eclipse projects > finish 

open terminal > click on small blue - white box > choose local terminal > change the directory to your project directory >
> mvn clean 
>mvn install
>mvn package 
> mvn clean package (will create .jar file for )
after running it check for .jar file in the target folder 
you should get build succes in all
then right click on the project 
> run as > java Application > App > ok >proceed 
you should see Hello World ! in the console 



